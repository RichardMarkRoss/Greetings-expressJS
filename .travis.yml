language: node_js
node_js:
  - "v8.9.4"
before_script:
  - psql -c "CREATE DATABASE hold_name;" -U postgres
  - psql -c "CREATE TABLE hold_name(id serial not null primary key, names text not null, counter int not null);" -U postgres
  - psql -c "CREATE USER coder WITH PASSWORD 'pg123';" -U postgres
  